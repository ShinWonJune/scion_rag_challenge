ScienceON AI Challenge에서 학술 검색 기반 RAG 시스템을 설계·구현해 우승했습니다.

[과제]
학술 검색 플랫폼(ScienceON)과 RAG 시스템을 연계하여 근거 문서 기반으로 자연어 질의에 대한 응답을 생성하는 시스템 설계 및 개발
- 제한된 GPU 자원 환경(~50GB VRAM)에서 정확한 문서 수집, 근거 문서 기반 컨텍스트 구성, 형식화된 답변 생성을 수행하는 end-to-end 파이프라인 구축

[역할]
- 검색어 생성 전략 설계 및 구현
- 검색식 구성 연산자 성능 실험 설계 및 수행
- 다국어 임베딩 모델 벤치마크 조사 및 도입 
- "질의 재구성 → 검색 플랫폼 질의 → 문서 수집 및 임베딩" 부분의 구현

[문제 정의]
1. 검색어 문제: 학술 검색 플랫폼에 비친화적인 자연어 질의
2. 교차언어 문제: 질의와 정답 문서의 언어 불일치

[문제 대응]
1. 검색어 문제: 논리 연산자를 활용한 키워드 기반 검색식 생성 전략 도입
- LLM(Gemini 2.5 Flash)으로 자연어 질의에서 한/영 각각의 핵심 키워드를 추출하고 중요도 순으로 정렬
- 복합 키워드는 단어 단위로 분리하고, 중요도 기준 상위 10개 키워드를 검색식 구성에 사용
- OR 중심의 키워드 검색식을 구성 (논리 연산자별 검색 성능을 비교하여 최적 전략을 선택)
- 상위 중요도 키워드부터 단계적으로 조합하여 검색 결과가 질의의 핵심에서 벗어나지 않도록 설계 (ex. 검색어A = a|b|c, 검색어B = a|b, ..)

2. 교차언어 문제: 다국어 임베딩 모델을 파이프라인에 도입
- MMTEB 및 MTEB 리더보드를 참고하여 모델 후보를 도출한 뒤 한국어, 영어 혼합 환경에서 성능을 비교
- 논문 초록의 임베딩을 위하여 Long-context 지원 다국어 임베딩 모델 고려
- 최종적으로 gte-multilingual-base 모델을 채택해 교차언어 문제 보완


[논리 연산자 검색식 성능 평가]
목적: OR, AND 논리연산자 구성에 따른 검색식 성능 평가
방법: ScienceON AI Challenge의 50개 질의를 대상으로, 각 질의 별 OR만 사용하는 조건(AND 0개)부터 AND만 사용하는 조건(AND 9개)까지 AND 연산자 수를 0~9개로 변화시키며 문서 수집 성능을 비교
- 평가 지표는 검색 문서 내 정답 문서 포함율과 평균 수집 문서 수로 설정
결과: 
- OR만 사용한 조건(AND 0개)에서 정답 문서 포함율이 약 88.5%로 가장 높았고, AND 1개 조건도 약 88.0% 수준이었으나 AND를 2개 이상 포함하면 약 80.4~84.0% 수준으로 하락
- OR만 사용한 조건의 평균 수집 문서 수는 약 1,450개로, AND 포함 조건의 약 2,000~2,200개보다 적어 비용 효율성이 높음
- AND 연산자를 포함하면 더 많은 문서를 수집하더라도 정답 문서 포함율이 개선되지 않았고, 특정 키워드 결합이 과도하게 강조되어 검색 결과가 핵심 주제에서 벗어나는 경향을 확인
- 위 실험을 근거로 최종 검색 전략은 AND를 배제하고 OR 기반 다국어 키워드 확장 방식으로 결정

[구현]
- RAG 시스템 앞단에 검색어 생성, 플랫폼 검색, 검색 결과 수집, 다국어 임베딩 모듈을 결합하여 플랫폼 검색부터 응답 생성까지 이어지는 end-to-end 파이프라인을 구축
- 전체 파이프라인은 "다국어 키워드 추출 → OR 기반 검색식 생성 → ScienceON 문서 수집 및 중복 제거 → 다국어 임베딩 → 유사도 검색 상위 5개 문서 기반 답변 생성"의 구조로 구현
- 각 검색식마다 상위 10개 문서를 수집하고, 중복 문서와 핵심 정보가 누락된 문서를 제거해 최종 문서 집합을 구성
- 최종 답변은 대회 요구 형식(제목, 서론, 본론, 결론)에 맞춰 생성되도록 프롬프트를 설계
- 전 파이프라인을 기존 프레임워크(LangChain 등)에 의존하지 않고 직접 구현

[성과]
학술 검색의 제약과 교차언어 검색 문제를 함께 해결하는 RAG 파이프라인을 구축해 ScienceON AI Challenge 우승
